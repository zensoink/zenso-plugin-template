<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width={{ zenso.device.width }}, height={{ zenso.device.height }}, initial-scale=1.0">
    <title>{{ plugin.name }}</title>
</head>
<body>
    <div class="plugin-container">
        <img class="plugin-logo" src="{{ 'assets/logo.png' | asset_url }}" alt="{{ plugin.name }}">
        <h1>{{ plugin.name }}</h1>
        <p class="greeting">{{ plugin.description }}</p>
        <p>Autor: <strong>{{ plugin.author.name }}</strong></p>

        <div class="interactive-section">
            <button id="action-btn">Kliknij mnie (Test JS)</button>
            <span id="click-counter">0</span>
        </div>
    </div>
</body>
</html>
