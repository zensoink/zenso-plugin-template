{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schemas.zenso.ink/v1/zenso-config.schema.json",
  "title": "Zenso Plugin Config (source)",
  "description": "Source side of the Zenso plugin manifest contract. name/version/author/description/license are NOT declared here — they merge from package.json at build time. Field definitions mirror the manifest schema at https://schemas.zenso.ink/v1/plugin-manifest.schema.json.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "URL of the config schema this file conforms to."
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+/[a-z0-9-]+$",
      "description": "Unique plugin identifier in owner/repo form, e.g. \"zenso/plugin-starter\"."
    },
    "thumbnail": {
      "type": "string",
      "description": "Relative path to a preview image bundled with the plugin."
    },
    "schema_version": {
      "const": 1,
      "description": "Must match the schema major version in the URL."
    },
    "core_min": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Minimum Zenso core version required."
    },
    "capabilities": {
      "type": "array",
      "items": { "enum": ["script"] },
      "uniqueItems": true,
      "description": "Runtime capabilities. Currently only \"script\" (allows bundled .js files)."
    },
    "config_schema": {
      "type": "object",
      "description": "JSON Schema (draft-07) of the plugin instance configuration (configJson). Must declare type \"object\" and properties. Each property type must be renderable by the Zenso panel: string | number | boolean | array (optionally an array of objects).",
      "required": ["type", "properties"],
      "properties": {
        "type": { "const": "object" },
        "properties": {
          "type": "object",
          "description": "Map of config field name to field definition. Panel renders one form field per entry.",
          "additionalProperties": {
            "type": "object",
            "required": ["type"],
            "additionalProperties": false,
            "properties": {
              "type": {
                "enum": ["string", "number", "boolean", "array"],
                "description": "Field type. Rendered as text, number input, toggle, or dynamic list."
              },
              "description": { "type": "string" },
              "default": {
                "description": "Pre-filled value shown in the panel form."
              },
              "format": {
                "type": "string",
                "description": "Value format hint. \"uri\" and \"color\" are rendered by the panel (color = native picker). Planned: date, time, datetime, email."
              },
              "enum": {
                "type": "array",
                "minItems": 1,
                "description": "Allowed values; renders as a dropdown."
              },
              "minimum": { "type": "number" },
              "maximum": { "type": "number" },
              "items": {
                "type": "object",
                "required": ["type"],
                "additionalProperties": false,
                "properties": {
                  "type": { "enum": ["string", "number", "boolean", "object"] },
                  "format": {
                    "type": "string",
                    "description": "Value format hint. \"uri\" and \"color\" are rendered by the panel (color = native picker)."
                  },
                  "description": { "type": "string" },
                  "default": {},
                  "enum": { "type": "array", "minItems": 1 },
                  "minimum": { "type": "number" },
                  "maximum": { "type": "number" },
                  "properties": {
                    "type": "object",
                    "description": "For object items: map of sub-field name to field definition (same shape as a top-level field).",
                    "additionalProperties": {
                      "type": "object",
                      "required": ["type"],
                      "additionalProperties": false,
                      "properties": {
                        "type": { "enum": ["string", "number", "boolean", "object"] },
                        "format": { "type": "string" },
                        "description": { "type": "string" },
                        "default": {},
                        "enum": { "type": "array", "minItems": 1 },
                        "minimum": { "type": "number" },
                        "maximum": { "type": "number" }
                      }
                    }
                  },
                  "required": {
                    "type": "array",
                    "items": { "type": "string" },
                    "description": "Required sub-field keys for object items."
                  }
                },
                "description": "For arrays: the type of each list item. Use \"object\" to model a list of structured rows (e.g. feeds with url + color)."
              }
            }
          }
        },
        "required": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Config field keys that must be present in configJson."
        },
        "additionalProperties": {
          "enum": [true, false],
          "description": "Whether unknown configJson keys are allowed (default: false in JSON Schema)."
        }
      }
    },
    "data_sources": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "type": {
            "type": "string",
            "minLength": 1,
            "description": "Data source handler. Currently supported: \"ics\"."
          },
          "config": {
            "type": "object",
            "description": "Source-specific options, e.g. references to configJson field names."
          }
        },
        "required": ["id", "type"]
      }
    }
  },
  "required": ["id", "schema_version", "core_min", "config_schema"]
}
